# 当前阶段命令清单：circle15 启动瞬态修复复测（--startup-fade）
# 更新日期：2026-09-08
# 前置：conda activate spherical_joint；会话回传 M*_verification/data/

# ============================================================
# 背景：v7 真版在稳态/大角度/waypoints 全过，唯 circle15 启动前 2s
#   峰值 9.5°（v6 3.4°）阻塞。已加 --startup-fade（默认 1s 前馈渐入）。
# 对比：seg0=v6（无 fade）/ seg1=v7 真版 + fade
# ============================================================

# circle 15°/8s（核心复测：启动前 2s 峰值应 ≤ v6 的 3.4°）
python -m collect.orchestrator --ab --circle 15 8 --duration 60 --servo-port COM5 --force-port COM3 --calibration calibrations/rig1.json --controller-config configs/controller_v7.json --baseline-controller-config configs/controller_v6.json --startup-fade 1.0

# 若启动峰值仍大，可试 fade 延长到 1.5~2s：
python -m collect.orchestrator --ab --circle 15 8 --duration 60 --servo-port COM5 --force-port COM3 --calibration calibrations/rig1.json --controller-config configs/controller_v7.json --baseline-controller-config configs/controller_v6.json --startup-fade 2.0

# circle 20°/10s（顺带复查 lr 散布尖峰）
python -m collect.orchestrator --ab --circle 20 10 --duration 60 --servo-port COM5 --force-port COM3 --calibration calibrations/rig1.json --controller-config configs/controller_v7.json --baseline-controller-config configs/controller_v6.json --startup-fade 1.0

# 每条跑完评测
python -m valuation.evaluate <session_dir> --all

# ============================================================
# 判据
# ============================================================
# 1) circle15 启动(0-2s) fb 峰值 ≤ v6 的 ~3.4°（原 9.5° 应被 fade 压下来）
# 2) circle15 稳态(t>5s) fb 保持 −8% 改善（fade 不应伤害稳态）
# 3) circle20 保持 fb MAE −19% 改善
